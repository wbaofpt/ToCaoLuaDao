DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=tocaoluadao
PORT=3001

# Optional SMTP settings. Without these, submissions are still saved and the
# server logs that email delivery is not configured.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASSWORD=your-smtp-password
SMTP_FROM="Tố Cáo Lừa Đảo <no-reply@example.com>"
